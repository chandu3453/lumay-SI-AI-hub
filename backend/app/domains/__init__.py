"""
LuMay SMART Insurance AI Hub — Domain Packages.

Each sub-package is a bounded domain following Domain-Driven Design principles.
Domains are self-contained with their own constants, models, schemas, events,
exceptions, repositories, services, and routers.

All domain routers are registered via lazy import in app.main to allow
incremental domain enablement.
"""
